# Runtime mode
NEXT_PUBLIC_APP_ENV=prod

# Backend API origin.
NEXT_PUBLIC_BACKEND_URL=https://api.gods-eye.in

# Public integrations used by the frontend.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
NEXT_PUBLIC_CALENDLY_URL=https://calendly.com/daftarroute123/intro-call

# Keep cloud credentials in a private, server-only env file if they are needed later.
